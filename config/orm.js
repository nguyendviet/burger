var connection = require('../config/connection.js');

function selectAll() {
    // to do
};

function insertOne() {
    // to do
};

function updateOne() {
    // to do
};

var orm = {
    all: selectAll(),
    insert: insertOne(),
    update: updateone()
};

module.exports = orm;