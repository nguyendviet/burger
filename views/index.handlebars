<h1>All burgers</h1>

<h2>Available burgers</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{burger_name}}
        {{/unless}} 
    {{/each}}
</ul>

<h2>Eaten burgers</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{burger_name}}
        {{/if}} 
    {{/each}}
</ul>